
# Egregor Mobile — Run Checklist (Windows + Expo + Supabase)

This checklist is the **single source of truth** to run and verify the app end-to-end after setup.

---

## 0) Prerequisites

- Node 20+
- npm 10+ or pnpm
- Expo Go on phone
- Supabase project created
- SQL schema + RLS + function from previous files already applied

---

## 1) Install dependencies

From `mobile/`:

```powershell
npm install
